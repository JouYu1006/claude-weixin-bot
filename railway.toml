[build]
  builder = "nixpacks"
  buildCommand = "npm install && npm run build"
  startCommand = "npm start"

[service]
  healthcheckPath = "/"
  port = 8080

[deploy]
  numReplicas = 1
  restartPolicyType = "always"
