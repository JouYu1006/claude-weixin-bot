[build]
  builder = "nixpacks"
  buildCommand = "npm install"
  startCommand = "node dist/index.js"

[service]
  healthcheckPath = "/"
  port = 8080

[deploy]
  numReplicas = 1
  restartPolicyType = "always"
